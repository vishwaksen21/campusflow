// CampusFlow frontend runtime config
//
// For Vercel (frontend) + Render (backend) deployments:
// 1) Set this to your Render backend URL (no trailing slash), then redeploy on Vercel.
//    Example: window.CAMPUSFLOW_API_BASE_URL = 'https://campusflow-backend.onrender.com';
// 2) For local dev where frontend is served by the backend, you can leave it empty.

window.CAMPUSFLOW_API_BASE_URL = '';
